{
  "compilerOptions": {
    "target": "es5", // Set the ECMAScript target version (adjust as needed)
    "module": "commonjs", // Module system (commonjs for Node.js)
    "strict": true, // Enable strict mode for type safety
    "outDir": "dist", // Output directory for compiled JavaScript (optional)
    "allowJs": true // Allow compilation of existing JavaScript files (optional)
  },
  "include": [
    "src"
  ] // Include pattern for TypeScript source files (optional)
}